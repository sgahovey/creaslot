{#
 # Bandeau de différenciation environnement
 #
 # Piloté par la variable Twig "app_environment_label" (définie dans
 # config/packages/twig.yaml depuis APP_ENVIRONMENT_LABEL).
 #
 # - APP_ENVIRONMENT_LABEL=preprod    : bandeau orange "PRÉ-PRODUCTION"
 # - APP_ENV=dev (sans label preprod) : bandeau bleu "DÉVELOPPEMENT"
 # - APP_ENVIRONMENT_LABEL=prod       : aucun bandeau
 #
 # À ne pas confondre avec APP_PREPROD, qui pilote le préfixe [PRÉPROD] du <title>
 # et la classe CSS du <body> (cf. base.html.twig), et non ce bandeau.
 #}
{% if app_environment_label is defined and app_environment_label == 'preprod' %}
    <div class="cs-env-banner cs-env-banner--preprod" role="status" aria-label="Environnement pré-production">
        <i class="bi bi-exclamation-triangle-fill" aria-hidden="true"></i>
        PRÉ-PRODUCTION — Les données de cet environnement ne sont pas réelles
    </div>
{% elseif app.environment == 'dev' %}
    <div class="cs-env-banner cs-env-banner--dev" role="status" aria-label="Environnement de développement">
        <i class="bi bi-code-slash" aria-hidden="true"></i>
        DÉVELOPPEMENT
    </div>
{% endif %}
